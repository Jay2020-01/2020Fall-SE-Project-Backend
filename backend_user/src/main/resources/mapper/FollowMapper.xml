<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.user.dao.FollowDao">

    <resultMap id="Follow" type="follow">
        <id property="followerId" column="follower_id"/>
        <result property="followingId" column="following_id"/>
        <result property="followTime" column="follow_time"/>
    </resultMap>

    <select id="getFollowList" parameterType="Integer" resultType="Integer">
        SELECT following_id
        FROM follow
        WHERE follower_id = #{id}
        ORDER by follow_time DESC
    </select>

    <select id="findConnection" parameterType="Integer" resultType="Date">
        SELECT follow_time FROM follow
        WHERE follower_id = #{followerId} and following_id = #{followingId}
    </select>

    <insert id="addConnection">
        INSERT INTO follow VALUES(#{followerId},#{followingId},#{time})
    </insert>

    <delete id="removeConnection">
        DELETE from follow
        where follower_id = #{followerId} and following_id = #{followingId}
    </delete>

    <select id="getFollowingCountById" resultType="Long">
        SELECT COUNT(1) FROM follow
        WHERE follower_id = #{followerId}
    </select>

    <select id="getFollowerCountById" resultType="Long">
        SELECT COUNT(1) FROM follow
        WHERE following_id = #{followingId}
    </select>

</mapper>
